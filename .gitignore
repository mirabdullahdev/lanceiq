# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# .env holds public build-time config and IS committed on purpose: the values
# are inlined into the browser bundle anyway. Real secrets live in Supabase
# function secrets and GitHub repository secrets, never in this repo.
.env.*.local

# Supabase CLI link state — machine-local, not shared config
supabase/.temp/
supabase/.branches/

# Cloudflare
.wrangler/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
